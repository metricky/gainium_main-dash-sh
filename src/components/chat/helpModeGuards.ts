// Chat help-mode guard stub. Allows everything when chat is absent.

export const isHelpModeAllowedToolStatus = (
  _toolName?: string,
  _toolDescription?: string,
  _message?: string
): boolean => true;
